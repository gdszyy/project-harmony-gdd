# Project Harmony: 2D/3D 架构认知偏差根因分析报告

**作者：** Manus AI (Architect Analyst)
**日期：** 2026-03-06

## 1. 执行摘要

本报告旨在诊断 Project Harmony 项目中近期出现的“将 3D 渲染架构错误回退为 2D”问题的核心原因。通过对核心设计文档（GDD）、技术方案文档、代码库引用统计以及 Git 提交历史的深入分析，我们发现该问题并非单一开发者的失误，而是由**文档体系的自相矛盾**、**代码库中 2D 遗留资产的压倒性比例**以及**核心设计文档的表述模糊**共同导致的系统性认知偏差。

## 2. 问题时间线与关键提交分析

通过追踪 Git 提交历史，我们重构了项目渲染架构演进与回退的时间线：

### 2.1. 架构演进期 (2026-02-09 至 2026-02-11)
*   **2026-02-09**: 提交 `7d2129b` 和 `49029e9` 引入了《美术与VFX方向总文档》和《美术框架实施方案》，首次提出了“3D 场景 + 正交投影摄像机”的混合渲染方案。
*   **2026-02-11**: 提交 `2cc864e` 正式实现了 2.5D 混合渲染桥接架构（`RenderBridge3D`），确立了“2D 逻辑 + 3D 渲染”的核心模式。

### 2.2. 认知偏差与回退期 (2026-03-06)
在 2026 年 3 月 6 日，连续发生了三次针对 Codex（图鉴）系统的提交，将原本正确的 3D 预览错误地回退为了 2D：
*   **提交 `33b1fa9`**: `fix(codex): 将敌人预览从 3D 改为 2D [v6.1]`。开发者在提交信息中明确指出：“游戏中所有敌人实际上是 2D 实现的（根节点为 CharacterBody2D，视觉节点为 Polygon2D + 2D Shader）... 这导致 Codex 预览与游戏内实际视觉完全不一致。”
*   **提交 `23b2587`**: `Add results for task tsk-5e06ae6a-de2: 修复 Codex 图鉴敌人预览 3D/2D 不一致问题`。
*   **提交 `b945909`**: `fix(codex): 将敌人预览从 3D 改为 2D（SubViewport + Camera2D）`。彻底删除了约 1594 行的 3D 模型构建代码。

这些提交表明，开发者在修复“视觉不一致”问题时，基于对代码现状的观察（敌人实体是 2D 的），得出了“项目是纯 2D”的错误结论，从而将原本符合 3D 渲染架构规划的 Codex 预览降级为 2D。

## 3. 矛盾文档清单与分析

项目文档库中存在严重的技术方案冲突，这是导致开发者产生认知偏差的直接原因。

### 3.1. 核心矛盾点：纯 2D vs 2.5D 混合渲染

| 文档名称 | 核心主张 | 状态/影响 |
| :--- | :--- | :--- |
| **`Docs/ART_IMPLEMENTATION_FRAMEWORK.md`** | **坚持纯 2D 架构**。明确声明：“不采纳美术方案中建议的'3D 场景 + 正交投影'方案，而是坚持并增强现有的纯 2D 渲染架构。” | **严重误导**。该文档详细论证了不迁移到 3D 的理由，并主张使用 2D 的 `WorldEnvironment`。 |
| **`Docs/Art_And_VFX_Direction.md`** | **3D 场景 + 正交投影**。明确指出：“尽管游戏是 2D 俯视角玩法，我们采用 3D 场景 + 正交投影摄像机的混合方案。” | **正确但被忽视**。这是项目的美术顶层设计，但其技术主张与框架实施文档冲突。 |
| **`Docs/ART_IMPLEMENTATION_2.5D_BRIDGE.md`** | **2.5D 混合渲染桥接**。详细阐述了 `RenderBridge3D` 的实现：“游戏的核心逻辑和物理交互依然在 2D 世界中运行，但渲染过程被桥接到一个独立的 3D 场景中。” | **最新且正确**。这是项目当前的实际架构目标，但未能有效覆盖或废弃旧文档。 |

### 3.2. GDD 核心文档的表述模糊

在核心的 `GDD.md` 中，虽然在第 7 章提及了《美术与VFX方向总文档》，但在描述核心玩法时，多次使用了可能暗示纯 2D 的表述（如“俯视角”、“弹幕发射”）。更重要的是，GDD 缺乏对“2D 逻辑 + 3D 渲染”这一特殊架构的明确、醒目的声明，导致开发者在查阅 GDD 时无法第一时间建立正确的架构认知。

## 4. 代码库 2D/3D 引用统计分析

代码库的现状是误导开发者的另一个关键因素。我们对项目中的节点类型引用进行了全面统计：

### 4.1. 节点引用数据对比

| 节点类型类别 | 2D 节点引用总数 | 3D 节点引用总数 | 比例 (2D : 3D) |
| :--- | :--- | :--- | :--- |
| **代码文件 (`.gd`, `.tscn`)** | 1,233 处 | 598 处 | 约 2.06 : 1 |
| **文档文件 (`.md`)** | 342 处 | 240 处 | 约 1.43 : 1 |
| **核心实体节点** | `CharacterBody2D`: 97<br>`Polygon2D`: 288<br>`Node2D`: 332 | `CharacterBody3D`: 2<br>`MeshInstance3D`: 194<br>`Node3D`: 75 | 压倒性偏向 2D |

### 4.2. 数据洞察

统计数据揭示了一个残酷的现实：**在代码层面，Project Harmony 看起来就是一个 2D 游戏**。
由于采用了“2D 逻辑 + 3D 渲染”的桥接架构，所有的物理实体（玩家、敌人、碰撞体）都必须使用 2D 节点（如 `CharacterBody2D`, `Area2D`）。此外，由于 3D 渲染代理（Proxy）的全面替换尚未完成，大量敌人仍在使用早期的 `Polygon2D` 进行视觉呈现（如 `enemy_static.tscn`）。

当后续开发者（如负责修复 Codex 的 Agent）检查敌人场景文件时，他们看到的是纯粹的 2D 节点树。基于“所见即所得”的直觉，他们自然会得出“这是一个 2D 游戏”的结论，进而认为 Codex 中的 3D 预览是错误的。

## 5. 核心根因结论

综合上述分析，Project Harmony 被错误认为是 2D 项目的核心原因可归结为以下三点：

1.  **技术文档的致命冲突**：`ART_IMPLEMENTATION_FRAMEWORK.md` 明确主张“坚持纯 2D”，与后续的 2.5D 桥接架构文档产生直接冲突。旧文档未被标记为废弃，导致开发者获取了错误的技术指导。
2.  **“2D 逻辑 + 3D 渲染”架构的认知门槛**：这种桥接架构在 Godot 开发中并非主流。代码库中充斥着大量的 2D 物理和逻辑节点（1233 处引用），掩盖了 3D 渲染层的存在。开发者在没有充分上下文的情况下，极易被代码表象误导。
3.  **视觉资产迁移的滞后**：虽然架构上引入了 `RenderBridge3D`，但实际的敌人场景（如 `enemy_static.tscn`）仍在使用 `Polygon2D` 作为视觉表现，尚未完全迁移到 3D 代理渲染。这种“半成品”状态直接导致了 Codex 预览的视觉不一致，触发了错误的修复行为。

## 6. 建议修复方向

为了彻底解决这一认知偏差问题，防止未来再次发生架构回退，建议采取以下修复措施：

### 6.1. 文档体系清理与统一
*   **废弃冲突文档**：立即将 `Docs/ART_IMPLEMENTATION_FRAMEWORK.md` 标记为 `[DEPRECATED]` 或移至归档目录，并在文件头部添加醒目声明，指向最新的 2.5D 桥接文档。
*   **更新 GDD 声明**：在 `GDD.md` 的“游戏概述”或“美术与视觉效果”章节，增加一段关于“2.5D 混合渲染架构（2D 逻辑 + 3D 渲染）”的明确、加粗的声明。
*   **建立架构共识文档**：编写一份简短的《架构必读指南》，强制所有新加入的开发者（或 Agent）在修改代码前阅读，明确物理层与渲染层的分离关系。

### 6.2. 代码与资产的彻底迁移
*   **加速 3D 视觉代理替换**：全面推进敌人和玩家视觉资产的 3D 化。将 `enemy_*.tscn` 中的 `Polygon2D` 视觉节点移除，统一通过 `RenderBridge3D` 注册并生成 3D 渲染代理（如 `MeshInstance3D` 或 `MultiMeshInstance3D`）。
*   **修复 Codex 预览**：在完成敌人 3D 视觉代理的迁移后，重新将 Codex 的预览系统改回 3D（`SubViewport` + `Camera3D`），并直接调用敌人的 3D 渲染代理，确保图鉴与游戏内视觉的绝对一致。

### 6.3. 架构层面的强约束
*   **重命名与隔离**：考虑将纯逻辑的 2D 场景与包含视觉表现的场景在目录结构上进行更严格的隔离，例如将逻辑实体放入 `logic/` 目录，将渲染代理放入 `visuals/3d/` 目录，从结构上暗示其分离关系。
