# godot_project/scripts/data/codex_data.gd 函数索引

> 自动生成于 2026-05-08 | 总行数: 1164 | 函数数: 10 | 语言: gdscript
> **本文件由 code-indexer 兼容脚本自动生成，严禁手动编辑。**

## 函数 / 类型列表

| 名称 | 类型 | 签名 / 参数 | 备注 |
|------|------|-------------|------|
| `CodexData` | class_name | `CodexData()` |  |
| `Volume` | enum | `Volume()` |  |
| `UnlockType` | enum | `UnlockType()` |  |
| `Rarity` | enum | `Rarity()` | 巨型函数 |
| `get_volume_entries` | static_func | `get_volume_entries(volume: Volume)` |  |
| `find_entry` | static_func | `find_entry(entry_id: String)` |  |
| `get_demo_config` | static_func | `get_demo_config(entry_id: String)` |  |
| `has_demo` | static_func | `has_demo(entry_id: String)` |  |
| `get_volume_total` | static_func | `get_volume_total(volume: Volume)` |  |
| `get_total_entries` | static_func | `get_total_entries()` |  |

## 巨型函数内部节点 (@section)

| 节点 | 说明 | 定位方式 |
|------|------|----------|
| 无 | - | - |

## 定位提示

本索引不记录行号。定位函数请使用：

```bash
grep -n "函数名" godot_project/scripts/data/codex_data.gd
```
