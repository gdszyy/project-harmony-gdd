# godot_project/scripts/data/music_data.gd 函数索引

> 自动生成于 2026-05-08 | 总行数: 1270 | 函数数: 16 | 语言: gdscript
> **本文件由 code-indexer 兼容脚本自动生成，严禁手动编辑。**

## 函数 / 类型列表

| 名称 | 类型 | 签名 / 参数 | 备注 |
|------|------|-------------|------|
| `MusicData` | class_name | `MusicData()` |  |
| `Note` | enum | `Note()` |  |
| `WhiteKey` | enum | `WhiteKey()` |  |
| `BlackKey` | enum | `BlackKey()` |  |
| `ChordType` | enum | `ChordType()` |  |
| `SpellForm` | enum | `SpellForm()` |  |
| `ModifierEffect` | enum | `ModifierEffect()` |  |
| `ChordFunction` | enum | `ChordFunction()` |  |
| `RhythmPattern` | enum | `RhythmPattern()` |  |
| `FatigueLevel` | enum | `FatigueLevel()` |  |
| `ChapterTimbre` | enum | `ChapterTimbre()` |  |
| `ElectronicVariant` | enum | `ElectronicVariant()` |  |
| `InscriptionRarity` | enum | `InscriptionRarity()` |  |
| `TimbreType` | enum | `TimbreType()` | 巨型函数 |
| `build_scale` | static_func | `build_scale(root: int, scale_name: String)` |  |
| `TonalMode` | enum | `TonalMode()` | 巨型函数 |

## 巨型函数内部节点 (@section)

| 节点 | 说明 | 定位方式 |
|------|------|----------|
| 无 | - | - |

## 定位提示

本索引不记录行号。定位函数请使用：

```bash
grep -n "函数名" godot_project/scripts/data/music_data.gd
```
