# CLAUDE.md

本文档是 Claude/Claude Code 进入 `project-harmony-gdd` 仓库时的工作入口。仓库已有的 `AGENTS.md` 是当前 Agent 协作规范的主入口；Claude 必须先读取本文件，再读取并遵守 `AGENTS.md` 及其指向的模块规则。若本文件与 `AGENTS.md` 存在不一致，应以更严格的约束为准，并在必要时同步更新两处文档。

## 1. 当前 Agent 规范索引

`AGENTS.md` 定义了仓库的全局协作规范、事实来源、上下文防污染策略、编辑策略、模块规则入口以及流程洞察维护要求。Claude 处理任何任务前，必须把 `AGENTS.md` 视为当前 Agent 规范的事实来源。

| `AGENTS.md` 章节 | 主要内容 | Claude 执行要求 |
|---|---|---|
| 1. 项目事实来源 | 定义 `godot_project/TODO.md`、`GDD.md`、`DOCUMENTATION_INDEX.md`、`.cursor/rules/` 与自动索引的优先级。 | 修改实现、设计或文档状态时，先确认对应事实来源，避免把历史报告当作当前状态。 |
| 2. 快速导航 | 按任务类型指向 Godot、服务端、文档、资产、脚本、平衡工具、知识库与流程洞察入口。 | 开始任务前按任务范围读取对应规则文件，不应仅凭根目录文档直接改动。 |
| 3. 上下文防污染策略 | 标记 `Archive/`、历史任务目录与结果目录默认跳过。 | 除非用户明确要求追溯历史或任务需要核验历史决策，否则不得主动读取归档与旧任务沉淀。 |
| 4. 全局编辑策略 | 规定活文档契约、不同修改规模的编辑策略以及大型文件索引使用方式。 | 修改架构、API、核心玩法、关键数据模型或跨模块流程时，必须同步更新相关规则和状态文档。 |
| 5. 全局禁止行为 | 禁止误用归档内容、遗漏 `godot_project/TODO.md` 同步、泄露凭证、绕过既有 Godot 架构或手改机器索引。 | 提交前检查是否触犯禁止项；需要更新自动索引时运行索引脚本，而不是手工编辑生成文件。 |
| 6. 模块规范索引 | 列出 `.cursor/rules/global.md`、`godot_project.md`、`server.md`、`Docs.md`、`Assets.md`、`Scripts.md`、`BalanceKit.md` 与 `knowledge-base.md`。 | 按改动范围读取并遵守相关模块规则。 |
| 7. 流程洞察与代码索引维护 | 要求记录非直观耦合、重复踩坑风险和跨模块流程，并维护代码级索引。 | 发现可复用经验或修改代码结构后，补充流程洞察并刷新索引。 |

## 2. 强制测试优先开发流程

本仓库所有涉及逻辑、玩法、系统行为、数据处理、状态流转或可验证缺陷修复的改动，必须使用 **Test-Driven Development**。在测试定义完成之前，禁止编写或修改任何生产逻辑代码。允许的前置工作仅包括只读分析、需求澄清、测试目录准备、行为描述草稿以及不含生产逻辑的测试骨架。

> **硬性约束：先行为、再测试、后实现。** 任何 Claude 任务都不得跳过行为注释确认，也不得在尚未定义测试前直接修改 Godot 脚本、服务端业务代码或其他生产逻辑。

### 2.1 Godot 测试框架与目录

Godot 相关测试必须使用 **GUT（Godot Unit Test）**。测试代码必须放在 Godot 项目的 `res://test/unit/` 路径下，对应仓库文件系统路径为 `godot_project/test/unit/`。除非用户明确要求迁移或整理旧测试，否则不得把新的单元测试写入 `godot_project/scripts/tests/`、`godot_project/scenes/tests/` 或其他历史测试目录。

| 测试类型 | 规定位置 | 说明 |
|---|---|---|
| Godot 单元测试 | `godot_project/test/unit/` | 使用 GUT 编写，文件命名应能反映被测模块或行为。 |
| 行为确认骨架 | `godot_project/test/unit/` | 在正式测试代码之前创建，只包含 Gherkin 行为注释与测试函数名。 |
| 非 Godot 测试 | 按对应模块既有规范 | 若涉及服务端或脚本，应先读取相关 `.cursor/rules/`，并保持同样的测试优先约束。 |

### 2.2 正式测试前的 BDD 行为确认门禁

在编写任何正式测试代码之前，必须先遵循 **Behaviour-Driven Development** 实践，使用 **Gherkin schema** 创建一个空测试文件。该文件只能包含 `Given-When-Then` 模式的行为注释和测试函数名，用于让用户确认预期行为是否正确。

空测试文件必须满足以下要求：

1. 使用 `Feature`、`Scenario`、`Given`、`When`、`Then` 注释表达行为意图，必要时可使用 `And` 补充前置条件或结果约束。
2. 只声明测试函数名，不写断言、不写夹具、不写模拟对象、不写生产逻辑调用，也不写任何会形成实际验证逻辑的测试代码。
3. 行为注释完成后，必须使用 `AskUserQuestion` 向用户确认是否继续。
4. 在用户明确确认继续之前，禁止编写正式测试代码；在正式测试代码完成并能表达失败预期之前，仍禁止编写生产逻辑代码。

推荐的空测试骨架形态如下；实际文件应根据任务命名，但必须保持“只有行为注释和测试函数名”的意图，不得提前加入断言或实现细节。

```gdscript
# Feature: <被验证的功能或系统行为>
# Scenario: <具体行为场景>
# Given <初始上下文或前置条件>
# When <触发的玩家行为、系统事件或函数调用>
# Then <期望出现的可观察结果>
func test_<expected_behavior_name>():
    pass
```

如果当前执行环境的工具名称不同，Claude 也必须通过等价的用户确认机制完成同一门禁；但在支持 `AskUserQuestion` 的环境中，必须优先使用 `AskUserQuestion`。

### 2.3 TDD 执行顺序

每个可验证改动必须按以下顺序推进，不得并行跳步或倒置顺序：

| 顺序 | 阶段 | 允许内容 | 禁止内容 |
|---|---|---|---|
| 1 | 行为草案 | 创建只含 Gherkin 注释和测试函数名的空测试文件。 | 编写断言、夹具、Mock、生产逻辑调用或实现代码。 |
| 2 | 用户确认 | 使用 `AskUserQuestion` 询问用户是否继续，并等待明确确认。 | 在确认前继续写正式测试或生产代码。 |
| 3 | 正式测试 | 使用 GUT 补全测试断言，使测试能表达当前失败预期。 | 为了让测试通过而提前修改生产逻辑。 |
| 4 | 最小实现 | 编写刚好满足测试的最小生产逻辑。 | 无测试覆盖的大范围重构或顺手改动。 |
| 5 | 重构与同步 | 在测试通过后重构，并同步 `godot_project/TODO.md`、相关规则或索引。 | 删除关键测试、绕过 GUT、遗漏活文档契约。 |

提交前必须确认：行为注释已经过用户确认，正式测试位于规定目录，生产逻辑改动有对应测试，且相关文档与索引按 `AGENTS.md` 的活文档契约完成同步。
